import { Meta } from '@storybook/addon-docs';

<Meta title="Kaltura Embed/kWidget/Code Sample" />

# Code

```html
<script src="https://www.kaltura.com/p/{partnerID}/sp/{partnerID}
	00/embedIframeJs/uiconf_id/{uiConfID}/partner_id/{partnerID}"></script>

<script>
	window.kWidget.embed({
		targetId: 'wrapper',
		wid: `_${partnerID}`,
		uiconf_id: uiConfID,
		entry_id: mediaID,
		flashvars: {
			autoPlay: false,
		},
	});
</script>

<div id="wrapper"></div>
```
